---
/**
 * One system on the Intro tab: a three-box flow strip (in → runs → out), three one-line
 * points (what · why · result), and a stat row. Every number shown already lives
 * elsewhere on the site.
 */
interface Stat {
  num: string;
  label: string;
}
interface Props {
  num: string;
  title: string;
  href: string;
  points: string[];
  flow: { input: string; process: string; output: string };
  stats: Stat[];
}
const { num, title, href, points, flow, stats } = Astro.props;
---

<article class="pb">
  <header class="pb__head">
    <span class="pb__num num">{num}</span>
    <h2 class="pb__title"><a href={href}>{title}</a></h2>
  </header>

  <div class="pb__flow" aria-label={`${title}: inputs, process, outputs`}>
    <div class="pb__box"><span class="pb__boxlabel">In</span>{flow.input}</div>
    <span class="pb__arrow" aria-hidden="true">→</span>
    <div class="pb__box pb__box--core"><span class="pb__boxlabel">Runs</span>{flow.process}</div>
    <span class="pb__arrow" aria-hidden="true">→</span>
    <div class="pb__box"><span class="pb__boxlabel">Out</span>{flow.output}</div>
  </div>

  <ul class="pb__points">
    {points.map((p) => <li>{p}</li>)}
  </ul>

  <ul class="pb__stats" aria-label="At a glance">
    {stats.map((s) => (
      <li><b class="num">{s.num}</b><span>{s.label}</span></li>
    ))}
  </ul>
</article>

<style>
  .pb {
    border-top: 1px solid var(--rule);
    padding: var(--s-6) 0 var(--s-7);
  }
  .pb__head {
    display: flex;
    align-items: baseline;
    gap: var(--s-4);
    margin-bottom: var(--s-5);
  }
  .pb__num {
    font-family: var(--mono);
    font-size: var(--t-12);
    color: var(--muted-2);
    letter-spacing: 0.08em;
  }
  .pb__title {
    font-family: var(--serif);
    font-weight: 400;
    font-size: clamp(24px, 3vw, 32px);
    letter-spacing: -0.015em;
    margin: 0;
    flex: 1;
  }
  .pb__title a { color: var(--fg); }
  .pb__title a:hover { color: var(--accent); text-decoration: none; }

  /* ---- flow strip ---- */
  .pb__flow {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1.2fr) auto minmax(0, 1fr);
    gap: var(--s-3);
    align-items: stretch;
    margin: 0 0 var(--s-5);
  }
  .pb__box {
    border: 1px solid var(--rule);
    background: var(--bg);
    padding: var(--s-3) var(--s-4);
    font-family: var(--sans);
    font-size: var(--t-14);
    line-height: 1.45;
    color: var(--fg-soft);
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .pb__box--core {
    border-color: var(--accent);
    background: var(--accent-soft);
    color: var(--fg);
  }
  .pb__boxlabel {
    font-family: var(--mono);
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--muted-2);
  }
  .pb__box--core .pb__boxlabel { color: var(--accent); }
  .pb__arrow {
    align-self: center;
    font-family: var(--mono);
    color: var(--muted-2);
    font-size: var(--t-17);
  }

  /* ---- three points ---- */
  .pb__points {
    margin: 0;
    padding-left: 1.1em;
    display: grid;
    gap: var(--s-2);
    max-width: var(--measure);
  }
  .pb__points li {
    font-family: var(--serif);
    font-size: var(--t-17);
    line-height: 1.5;
    color: var(--fg-soft);
  }
  .pb__points li::marker { color: var(--muted-2); }

  /* ---- stat row ---- */
  .pb__stats {
    list-style: none;
    margin: var(--s-5) 0 0;
    padding: var(--s-4) 0 0;
    border-top: 1px solid var(--rule-soft);
    display: flex;
    flex-wrap: wrap;
    gap: var(--s-6);
  }
  .pb__stats li { display: flex; flex-direction: column; gap: 2px; }
  .pb__stats b {
    font-family: var(--mono);
    font-size: clamp(18px, 2.2vw, 24px);
    font-weight: 500;
    color: var(--accent);
  }
  .pb__stats span {
    font-family: var(--sans);
    font-size: var(--t-12);
    color: var(--muted);
  }

  @media (max-width: 860px) {
    .pb__flow { grid-template-columns: minmax(0, 1fr); }
    .pb__arrow { transform: rotate(90deg); justify-self: center; }
    .pb__head { flex-wrap: wrap; }
  }
</style>
